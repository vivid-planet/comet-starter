import { Config } from "../site-configs";

export default {
    name: "Starter Main EN",
    contentScope: {
        domain: "main",
        language: "en",
    },
    domains: {
        local: "en.localhost:3000",
    },
} satisfies Config;
