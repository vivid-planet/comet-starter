import { Config } from "../site-configs";

export default {
    name: "Starter Main DE",
    contentScope: {
        domain: "main",
        language: "de",
    },
    domains: {
        local: "localhost:3000",
    },
} satisfies Config;
