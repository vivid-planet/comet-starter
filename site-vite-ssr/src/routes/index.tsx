/*
import { ComponentType } from "react";
import { BlockPreview, loader as blockPreviewLoader } from "./block-preview";
import { Page, loader as pageLoader } from "./page";


export const routes: Record<string, { component: ComponentType<any>, loader?: any }> = {
    page: {
        component: Page,
        loader: pageLoader
    },
    blockPreview: {
        component: BlockPreview,
        loader: blockPreviewLoader
    }
};
*/